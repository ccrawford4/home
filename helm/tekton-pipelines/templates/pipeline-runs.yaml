{{- range .Values.tasks }}
---
apiVersion: tekton.dev/v1alpha1
kind: PipelineRun
metadata:
  name: {{ .name }}
  namespace: tekton-pipelines
spec:
  pipelineRef:
    name: {{ .pipelineRef }}
  params:
  {{- range .params }}
    - name: {{ .name }}
      value: {{ .value }}
  {{- end }}
{{- end }}
