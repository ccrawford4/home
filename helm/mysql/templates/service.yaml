{{- range .Values.instances }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .name }}
  namespace: {{ $.Values.namespace }}
spec:
  ports:
  - port: {{ .port | default $.Values.defaults.port }}
  selector:
    app: {{ .name }}
  clusterIP: None
{{- end }}
